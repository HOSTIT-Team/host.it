<% if @event.is_invited?(current_user) || @event.user == current_user %>
  <div class="event-container">
    <div class="event-banner" style="background-image: <%=  "linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('#{cl_image_path(@event.banner.key)}')" if @event.banner.attached? %>" >
      <% if @event.user == current_user || current_user.admin? %>
        <%= link_to edit_event_path(@event), class: "button-create upload-button" do %>
          <i class="far fa-edit"></i>
        <% end %>
      <% end %>
    </div>

<<<<<<< HEAD
  <div class="event-guest-map">
    <div class="guest-list">
      <div class="guest-list-header">The Guests</div>
      <div class="slider-area">
        <% @event.invitations.each do |invitation| %>
          <% if invitation.receiver && (invitation.status == "pending" || invitation.status == "accepted") %>
            <div class="slide">
              <div class="guest-cards">
                  <% if invitation.receiver.avatar.attached? %>
                    <%= cl_image_tag invitation.receiver.avatar.key %>
                  <% else %>
                    <%= image_tag "placeholder-avatar.png"%>
                  <% end %>
              </div>
              <% if invitation.receiver && invitation.receiver.first_name && invitation.receiver.last_name %>
                <div class="guest-name"><%= link_to invitation.receiver.full_name, user_path(invitation.receiver) %></div>
              <% else %>
                <div class="guest-name"><%= link_to "Nemo", user_path(invitation.receiver) %></div>
              <% end %>
            </div>
           <% end %>
        <% end %>
      </div>
      <a href="" class="list-button button-invite">See list</a>
    </div>
    
      <div id="map"
        data-markers="<%= @marker.to_json %>"
        data-mapbox-api-key="<%= ENV["MAPBOX_API_KEY"]%>">
      </div>
=======
    <%= render "events/details" %>
>>>>>>> main

    <div class="guestlist-map-section">
      <div class="event-guestlist">
        <%= render "events/guest_list" %>
        <%= render "events/invite_modal" %>
        <div class="section-header">The Guests</div>
      </div>
      <%= render "events/location_map" %>
    </div>

    <div class="items-playlist" id='items-anchor' >
      <div class ='items-container'>
      <div class="section-header">The Bringlist</div>
        <%= render "shared/items_form" %>
      </div>
<<<<<<< HEAD
    </div>
  </div >
  <div class="event-chat" id="user-<%= current_user.id %>"> 
    <div class="chat-box" data-chatroom-id="<%= @event.chatroom.id %>">
      <% @event.messages.each do |message| %>
        <% if message.user == current_user %>
        <div class="message-container message-container-user" id="message-<%= message.id %>">
        <a href="linkto google <%= message.id %>,<%= message.id %>"></a>
        <% else %>
        <div class="message-container" id="message-<%= message.id %>">
        <% end %>
          <% unless current_user == message.user %>
            <% if message.user.avatar.attached? %>
              <div class="chat-avatar" style="background-image: url('<%= cl_image_path message.user.avatar.key %>')"></div>
            <% else %>
              <div class="chat-avatar" style="background-image: url('<%= image_path "placeholder-avatar.png" %>')"></div>
            <% end %>  
          <% end %>
          <% if message.user == current_user %>
          <div class="message message-user">
          <div class="message-content message-content-user">
          <%= message.content %>
            </div>
            <div class="message-username-time username-time-right">
          <% else %>
          <div class="message">
          <div class="message-content">
          <%= message.content %>
            </div>
            <div class="message-username-time">
          <% end %>
              <% if message.user.full_name %>
                <% unless message.user == current_user %>
                  <div class="message-username"><%= message.user.full_name %></div>
                <% else %>
                  <div class="message-username">You</div>
                <% end %>
              <% else %>
                <div class="message-username">Nemo</div>
              <% end %>
              <div class="message-time"><%= message.created_at %></div>
            </div>
          </div>
=======
      <div class ='playlist-container'>
        <div class="section-header">The Playlist</div>
        <div class="playlist">
          <iframe src="https://open.spotify.com/embed/playlist/0K0RsJ0xxaTDZIKQ1vszY3" width="400" height="500" frameborder="0" allowtransparency="true" allow="encrypted-media"></iframe>
>>>>>>> main
        </div>
      </div>
    </div >

    <div class="event-chat" id="user-<%= current_user.id %>"> 
      <%= render "events/chat" %>
  </div>

<% else %>
  <div class="event-error">
    <i class="fas fa-exclamation-triangle"></i>
    <div class="event-error-message">Sorry, but You are not invited to this event!</div>
    </div>
<% end %>